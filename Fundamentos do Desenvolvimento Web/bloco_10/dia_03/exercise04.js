const stringToUpperCase = (str) => str.toUpperCase();
const firstChar = (str) => str.charAt(0);
const concatString = (strA, strB) => strA.concat(strB);

module.exports = {
  stringToUpperCase,
  firstChar,
  concatString
}
